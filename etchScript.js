document.getElementsByClassName("container");
document.createElement("div");

const rows = 


function generateRow(numRow){
    const newDiv = document.createElement("div");
    for (r=0; r <= numRow; r++) {
        return newDiv;
    }
}

function generateCells(numCells) {
    const newCells = document.createElement("div");
    for(c=0; c < )
};

document.body.append("container");
document.container.append("newDiv")
//change the second append to append output of generative functions "grid"